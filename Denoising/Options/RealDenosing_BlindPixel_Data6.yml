# general settings
name: RealDenosing_BlindPixel_Data6
model_type: ImageCleanModel
scale: 1
num_gpu: 1
manual_seed: 100

# dataset and data loader settings
datasets:
  train:
    name: TrainSet
    type: Dataset_PairedImage_BlindPixel
    dataroot_gt: data6/train_sharp
    dataroot_lq: data6/train_blur
    in_ch: 1
    blind_weight: 0.0
    geometric_augs: true
    filename_tmpl: '{}'
    io_backend:
      type: disk

    use_shuffle: true
    num_worker_per_gpu: 8
    batch_size_per_gpu: 4
    pin_memory: true
    prefetch_mode: cuda

    # Progressive training (单阶段)
    mini_batch_sizes: [4]
    iters: [150000]
    gt_size: 384
    gt_sizes: [384]
    dataset_enlarge_ratio: 1

  val:
    name: ValSet
    type: Dataset_PairedImage_BlindPixel
    dataroot_gt: data6/val_sharp
    dataroot_lq: data6/val_blur
    in_ch: 1
    blind_weight: 0.0
    filename_tmpl: '{}'
    io_backend:
      type: disk

# network structures
network_g:
  type: Restormer
  inp_channels: 1
  out_channels: 1
  dim: 48
  num_blocks: [4, 6, 6, 8]
  num_refinement_blocks: 4
  heads: [1, 2, 4, 8]
  ffn_expansion_factor: 2.66
  bias: False
  LayerNorm_type: BiasFree
  dual_pixel_task: False

# path
path:
  pretrain_network_g: ~
  strict_load_g: true
  resume_state: ~

# training settings
train:
  total_iter: 150000
  warmup_iter: -1
  use_grad_clip: true
  use_fp16: true
  blind_weight: 0.0

  scheduler:
    type: CosineAnnealingRestartCyclicLR
    periods: [50000, 50000, 50000]
    restart_weights: [1, 1, 1]
    eta_mins: [0.000001, 0.000001, 0.000001]

  mixing_augs:
    mixup: false

  optim_g:
    type: AdamW
    lr: !!float 3e-4
    weight_decay: !!float 1e-3
    betas: [0.9, 0.999]

  pixel_opt:
    type: L1Loss
    loss_weight: 1
    reduction: mean

# validation settings
val:
  window_size: 8
  val_freq: 4000
  save_img: false
  rgb2bgr: false
  use_image: false
  max_minibatch: 8

  metrics:
    psnr:
      type: calculate_psnr
      crop_border: 0
      test_y_channel: false
    ssim:
      type: calculate_ssim
      crop_border: 0
      test_y_channel: false

# logging settings
logger:
  print_freq: 200
  save_checkpoint_freq: 200000
  use_tb_logger: true
  wandb:
    project: ~
    resume_id: ~

# dist training settings
dist_params:
  # backend: nccl
  # port: 29500
